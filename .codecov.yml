projects: *projects
  target: auto
  threshold: 10%
  if_not_found: success
  if_ci_failed: error
pathes: *patches
  only_pulls: true
  target: auto
  threshold: 10%

coverage:
  precision: 1
  round: down
  range: "70...100"

  status:
    project:
      default: off
      Linux-1.13.x:
        <<: *projects
      Linux-1.14.x:
        <<: *projects
      Linux-1.15.x:
        <<: *projects
      macOS-1.13.x:
        <<: *projects
      macOS-1.14.x:
        <<: *projects
      macOS-1.15.x:
        <<: *projects
    patch:
      default: off
      Linux-1.13.x:
        <<: *patches
      Linux-1.14.x:
        <<: *patches
      Linux-1.15.x:
        <<: *patches
      macOS-1.13.x:
        <<: *patches
      macOS-1.14.x:
        <<: *patches
      macOS-1.15.x:
        <<: *patches
    changes: true

flags:
  Linux-1.13.x:
    paths:
      - /
  Linux-1.14.x:
    paths:
      - /
  Linux-1.15.x:
    paths:
      - /
  macOS-1.13.x:
    paths:
      - /
  macOS-1.14.x:
    paths:
      - /
  macOS-1.15.x:
    paths:
      - /

comment:
  behavior: default
  require_changes: true
